import { Controller } from "@nestjs/common";

@Controller()
export class FormsController {
    // Get all forms (pagination)
    
    // Get one form (get detail)

    // Create form

    // Update form

    // Remove form (single or multi)
}